# specify path to citrus.conf or leave blank
# default is the same as CitrusCore
citrus_conf=/root/.citruscore/citrus.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
